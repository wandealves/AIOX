global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "aiox-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["aiox-api:8080"]
        labels:
          service: "aiox-api"
